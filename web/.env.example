# Base URL for the CircleK proxy API.
# Leave blank to use the Vite dev proxy (/api -> http://localhost:5180).
# Set to an absolute URL (e.g. https://proxy.example.com) for a deployed build.
VITE_API_BASE=
